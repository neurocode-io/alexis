{
  "name": "alexis",
  "version": "0.1.0",
  "description": "alexis for now",
  "main": "dist/index.js",
  "scripts": {
    "bootstrap": "cd client && npm install",
    "postbootstrap": "cd server && npm install",
    "waitForBackend": "sleep 5",
    "frontend": "cd client && npm run start",
    "server": "cd server && npm run start",
    "client": "run-s waitForBackend frontend",
    "start": "run-p server client",
    "test": "cd server && npm test"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}